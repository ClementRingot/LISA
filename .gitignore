node_modules/
dist/
.env
*.local
*.log
.DS_Store

# MTA build artifacts
mta_archives/

# MTA extension descriptor — real landscape files (the .example template stays
# tracked: the glob ends at `.mtaext`, so `mta-overrides.mtaext.example` is not matched)
mta-overrides*.mtaext
