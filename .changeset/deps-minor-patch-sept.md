---
'@lisa-mcp/server': patch
---

Raise the minimum versions of runtime dependencies: `@modelcontextprotocol/sdk` ^1.30.0, `jose` ^6.2.12, `undici` ^8.10.2, `zod` ^4.6.5. Installs from npm can no longer resolve older releases of these packages.
