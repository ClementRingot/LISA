{
  "name": "@lisa/core",
  "version": "0.1.0",
  "private": true,
  "description": "Transport-agnostic wire contract + Zod schemas for the LISA ZI18N_SERVICE (shared by the standalone MCP server and the ARC-1 extension)",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "biome check src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^4.1.9"
  }
}
